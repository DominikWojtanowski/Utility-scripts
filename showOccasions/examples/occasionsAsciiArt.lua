OCCASION_DATES = require("dates")

return {
  DATES_ASCII_ART = {
    -- OCCASIONS ASCII ART should be in format:
    -- [OCCASION_DATES.DATES["OCCASION NAME"] = [[
    --  <ASCII ART>
    -- ]]
  }
}
